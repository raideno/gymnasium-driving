# NOTES:

- [ ] Have better obstacle generation. Make sure the path is navigable.
- [ ] Commit repository and add documentation.
- [ ] Estimate the jerk and plot it.
- [ ] Show the different environments and the randomness of the sizes of the roads.
- [ ] What happens when there is an obstacle, does it try to brake or not ? Does it just go straight into it.
- [ ] Compute mean cross track error on an environment without obstacles.
- [ ] Modify detection_range of obstacles and put it a lower and more realistic value, currently it is set to 10 meters, we should put it to something like 5.

- [ ] Know when does the model react to an obstacle, do test environment for that, depending on the vehicle's speed, etc.
- [ ] Parametrize the reward function.

---

- [ ] Export the model into a python program in order to be integratable into the CARLA simulator simulator, add a command for that.
- [ ] Try to use the rosbag data in order to get the real car max acceleration, max steering, etc. In order to adapt to the ZOÉ. This way we can deploy the agent into the ZOE. To make everything compatible with zoé's inputs and outputs.

---

- [ ] Try to reduce oscillations by integrating that into the reward function.
- [ ] Integrate some term into the reward function in order to account for speed. Make the agent stop whenever the path isn't navigable by integrating some new term into the reward function.
